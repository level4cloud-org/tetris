name: CI Pipeline

on:
  push:
    branches:
      - main
      - babysitting
      - experiment
      - 'feature/**'
      - 'hotfix/**'

jobs:

  main:
    if: github.ref == 'refs/heads/main'
    uses: level4cloud-org/devops_library/.github/workflows/ci.yaml@main
    secrets: inherit
    with:
      INPUT_PARAM: "Hello World"
      MODEL_NAME: "Sales"
      PROJECT_NAME: "Salesproject"